// Keep this consistent with tsconfig.json at repo root.
// For notes and comments, see tsconfig.json at repo root.
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": "dist",
    "noUncheckedIndexedAccess": true
  },
  "include": ["src/**/*"]
}
