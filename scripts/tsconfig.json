{
  "extends": "common-tsconfig/tsconfig.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "noEmit": true,
    // TODO: Remove this when packages/integration removes browser specific code
    "lib": ["DOM"]
  },
  "include": ["./**/*.ts"],
  "exclude": ["./node_modules"]
}
