import styled from '@emotion/styled';
import { SECTION_PADDING_BOTTOM } from '../ui-constants';

const NodeBoxIncomingVariableSection = styled.div`
  padding: 0 10px;
  margin-bottom: ${SECTION_PADDING_BOTTOM}px;
`;

export default NodeBoxIncomingVariableSection;
