import styled from '@emotion/styled';
import { FormControl } from '@mui/joy';

const NodeFieldSectionFormControl = styled(FormControl)`
  margin-top: 10px;
  margin-bottom: 10px;
`;

export default NodeFieldSectionFormControl;
