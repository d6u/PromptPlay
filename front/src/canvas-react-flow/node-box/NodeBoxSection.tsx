import styled from '@emotion/styled';
import { SECTION_PADDING_BOTTOM } from '../ui-constants';

const NodeBoxSection = styled.div`
  padding: 0 10px;
  margin: ${SECTION_PADDING_BOTTOM}px 0;
`;

export default NodeBoxSection;
